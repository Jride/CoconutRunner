# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

source 'https://github.com/ITV/iOS-Podspecs.git'

target 'CoconutBasket' do
  # Comment the next line if you don't want to use dynamic frameworks

  # Pods for CoconutBasket
  pod 'FoundationExtended'

  target 'CoconutBasketTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
